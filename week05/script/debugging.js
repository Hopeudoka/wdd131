const pi = 3.14;
// let radius = 3;
let area = 0;
// area = radius * radius * pi;
// console.log(area);
// radius = 4;
// area = radius * radius * pi;
// console.log(area);

function circleArea(radius) {
    const area = radius * pi;
    return area
}

newArea = circleArea(3);
console.log(newArea);